diff --git a/include/wlr/types/wlr_keyboard.h b/include/wlr/types/wlr_keyboard.h
--- a/include/wlr/types/wlr_keyboard.h
+++ b/include/wlr/types/wlr_keyboard.h
@@ -93,0 +94,5 @@
+		/* Every explicit backend snapshot, before local XKB processing,
+		 * including unchanged masks. Data: wlr_keyboard_modifiers.
+		 * Key-derived modifiers do not emit this signal. */
+		struct wl_signal modifiers_input;
+
diff --git a/types/wlr_keyboard.c b/types/wlr_keyboard.c
--- a/types/wlr_keyboard.c
+++ b/types/wlr_keyboard.c
@@ -85,0 +86,7 @@
+	struct wlr_keyboard_modifiers input = {
+		.depressed = mods_depressed,
+		.latched = mods_latched,
+		.locked = mods_locked,
+		.group = group,
+	};
+	wl_signal_emit_mutable(&keyboard->events.modifiers_input, &input);
@@ -147,0 +155 @@
+	wl_signal_init(&kb->events.modifiers_input);
