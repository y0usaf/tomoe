diff --git a/xwayland/xwm.c b/xwayland/xwm.c
--- a/xwayland/xwm.c
+++ b/xwayland/xwm.c
@@ -2067 +2067,5 @@ static int x11_event_handler(int fd, uint32_t mask, void *data) {
-	if (mask & WL_EVENT_READABLE) {
+	/* Checked sources are dispatched with mask 0. XCB may have buffered
+	 * events while waiting for a synchronous reply, leaving its fd
+	 * unreadable. Drain that queue when Wayland checks the source too.
+	 */
+	if (mask == 0 || (mask & WL_EVENT_READABLE)) {
